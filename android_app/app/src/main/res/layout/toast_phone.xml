<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:padding="8dp"
    android:id="@+id/toast_phone_root"
    >
    <ImageView
        android:id="@+id/toast_phone_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_baseline_send_to_mobile_24"
        android:layout_marginEnd="8dp"
        />
    <TextView
        android:id="@+id/toast_failed_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Please contact our customer service: +46727741805"
        android:textSize="20dp"
        android:textColor="@color/theme"/>

</LinearLayout>